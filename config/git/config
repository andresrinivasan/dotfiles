[user]
	name = André Srinivasan
	useConfigOnly = true
	##signingkey = 23CFFEC9C545E50E
[core]
	hooksPath = ~/.config/git/hooks
	editor = vi
	pager = less -R
	autocrlf = input
[credential]
        helper = store
[commit]
	##gpgsign = true
[pull]
	rebase = true
[pretty]
	simple = format:%Cred%h%Creset:%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset
[format]
	pretty = simple
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[rebase]
	autoStash = true

# Enforce SSH, for my repos. Homebrew also uses GitHub but I want that to use HTTPS so that an SSH key isn't
# required from the ssh-agent.
[includeIf "gitdir:~/repos/"]
  path=config-use-ssh

# [url "https://github.com/Homebrew"]
#   insteadOf = git@github.com:Homebrew
#   insteadOf = https://github.com/Homebrew
# [url "https://github.com/eth-p/homebrew-software"]
#   insteadOf = git@github.com:eth-p/homebrew-software
#   insteadOf = https://github.com/eth-p/homebrew-software
# [url "https://github.com/brunoluiz/homebrew-tap"]
#   insteadOf = git@github.com:brunoluiz/homebrew-tap
#   insteadOf = https://github.com/brunoluiz/homebrew-tap

# [url "git@github.com:"]
#   insteadOf = https://github.com/
# [url "git@gitlab.com:"]
#   insteadOf = https://gitlab.com/
# [url "git@bitbucket.org:"]
#   insteadOf = https://bitbucket.org

